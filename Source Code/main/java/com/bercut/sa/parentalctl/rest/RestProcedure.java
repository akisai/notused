package com.bercut.sa.parentalctl.rest;

/**
 * Created by haimin-a on 25.03.2019.
 */
public enum RestProcedure {
    add_parent,
    add_children,
    delete_parent,
    delete_children,
    set_children
}
